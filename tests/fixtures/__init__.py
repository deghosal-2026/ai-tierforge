"""
Init for the test fixtures package.

This package contains:
- ``tiers.yaml`` — valid 3-tier config for testing
- ``tiers-invalid.yaml`` — broken config for validation tests
- ``tiers-custom-tiers.yaml`` — non-standard tier names for ordering tests
- ``mock_responses.py`` — pre-built ModelCall objects for test convenience
"""
